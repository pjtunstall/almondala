{
  "scripts": {
    "build:wasm": "wasm-pack build --target web --out-dir ./dist/wasm && mv ./dist/wasm/*.wasm ./public/wasm/ && mv ./dist/wasm/*.js ./public/wasm/ && mv ./dist/wasm/*.d.ts ./public/wasm/",
    "build:ts": "tsc",
    "build": "npm run build:wasm && npm run build:ts"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "undici-types": "^7.2.2"
  }
}
